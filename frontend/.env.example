# Main Quantum Studio backend (AI generation, auth, projects)
VITE_BACKEND_URL=http://localhost:5000

# Qiskit Metal Python bridge (component discovery, rendering, code generation)
# Start it with: cd editor/backend && python start.py
# Leave empty to disable bridge features in the Schematic Editor
VITE_BRIDGE_URL=http://localhost:8000
